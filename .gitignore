__pycache__/
*.pyc
*.pyo
.DS_Store
*.egg-info/
clew-native/target/
tera-pilot-native/target/
stderr.txt
.venv/
venv/
.pytest_cache/

# Agent learning-loop runtime state (marker files only, machine-local)
learnings/
tera_pilot_tui/learnings/

# Raw eval run logs are development artifacts, not versioned.
# The human-readable summary is eval/REPORT_*.md; the schema stays tracked.
eval/results/*.json
!eval/results/schema.json
# Per-run JSON logs from the `direct`/`api` drivers (P1.6 repeats) are
# development artifacts too — keep the whole agentic results dir out.
eval/results/agentic/
!eval/results/agentic/.gitkeep

# Working documents and local-only material — deliberately NOT pushed.
# (These were left out of past pushes on purpose; keep them out.)
AGENT_EVAL_AND_SECURITY_REPORT_*.md
GROQ_EVAL_REPORT.md
SESSION_REPORT_*.txt
V240_VERIFICATION_REPORT.txt
P0_IMPLEMENTATION.md
SECURITY_TEST_REPORT.md
TERA_PILOT_PRODUCT_READINESS.md
TERA_PILOT_PRODUCT_STRATEGY.md

docs/
loops/
payment.html

# Local bugfix/working notes inside the package.
tera_pilot/BUGFIXES_*.md

# Local eval report drafts (published summaries stay in README).
eval/REPORT_*.md
