"""A tiny greeting library with a versioned git history."""


def greet(name):
    """Return a greeting for ``name``."""
    return f"Hello, {name}!"


def shout(name):
    """Return an uppercase greeting for ``name``."""
    return f"HELLO, {name.upper()}!"
