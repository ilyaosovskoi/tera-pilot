# Core runtime dependencies for Tera Pilot v2.3.8
# Install: pip install -r requirements.txt
# For native acceleration (optional but recommended):
#   cd tera-pilot-native && maturin develop --release

# v2.2.0: GUI is served by a plain HTTP server (tera_pilot.web_server).
# No Qt / PySide6 dependency — point a browser at the printed URL.

# AI providers (used by tera_pilot/providers/*.py)
anthropic>=0.28.0
openai>=1.30.0
groq>=0.9.0

# Utilities
requests>=2.31.0
aiohttp>=3.9.0
pydantic>=2.6.0
toml>=0.10.2
pyyaml>=6.0
pygments>=2.18.0
rich>=13.7.0

# v2.0: full-screen terminal UI (tera_pilot_tui/, `python -m tera_pilot_tui`)
textual>=0.60.0

# v1.2.0: Office Worker section — built-from-scratch Python wrapper
# over these three well-known libraries. The Office Worker module
# (tera_pilot/office_worker.py) does NOT shell out to any external CLI and
# does NOT embed third-party agent code; it uses these libraries
# directly to create/edit .docx / .xlsx / .pptx files.
python-docx>=1.1.0
openpyxl>=3.1.0
python-pptx>=0.6.23

# v2.0: cryptography for EncryptedPromptStore (ChaCha20-Poly1305).
# Optional — falls back to XOR if not installed (loud warning logged).
cryptography>=42.0.0

# v2.0: optional Rust native extension (build with maturin, NOT installed via pip):
#   cd tera-pilot-native && maturin develop --release
# This builds and installs the `tera_pilot_native` Python extension from the Rust
# sources in tera-pilot-native/. Without it, pure-Python fallbacks are used
# (slower but functional).
